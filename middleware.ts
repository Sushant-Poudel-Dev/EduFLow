[{'@supabase/ssr': 'import { NextResponse'}, {'next/server";\n\n// Routes that are always public — no auth needed\nconst PUBLIC_ROUTES = [\n  "/login",\n  "/register",\n  "/auth/callback",\n  "/auth/signout': 'function isPublicRoute(pathname: string) {\n  return PUBLIC_ROUTES.some((route) => pathname.startsWith(route));'}, {'request': {'headers': 'request.headers'}}, {'cookies': {'data': {'loginUrl.searchParams.set("returnTo': 'pathname);\n    return NextResponse.redirect(loginUrl);'}, 'matcher': ['Match all routes except:\n     * - _next/static  (Next.js static files)\n     * - _next/image   (Next.js image optimization)\n     * - favicon.ico\n     * - Public asset files (svg, png, jpg, etc)\n     */\n    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']}}]